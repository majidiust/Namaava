Room.Router = Backbone.Router.extend({
    routes: {
        '': 'index'
    },

    index: function () {
        // if(console != 'undefined'){
        // 	console.log('the index page');
        // }
    }
});